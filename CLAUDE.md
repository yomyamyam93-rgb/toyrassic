# 토이라기 (Toyrassic) — 작업 규칙

Unity 6000.3.20f1 · URP · **새 Input System 전용**(옛 `Input.GetKey` 안 됨).
설계 정본은 별도 저장소 `yomyamyam93-rgb/game` 의 `방향전환_조립식_2026-07-23.md`.

## 개발자 정보

- 비개발자 1인 개발. **코드는 AI가 직접 파일을 수정한다** — 붙여넣기 시키지 않는다.
- 모든 작업의 종점은 **"유니티 열고 눈으로 보이는 결과"**. 안 보이는 진행은 진행이 아니다.

## ★손·무기는 반드시 씬에 실존시킨다 (2026-07-28 사용자 확정)

**새 무기를 추가할 때는 `HandRig/HandR`(또는 조준 무기면 `HandL`) 밑에 실제
오브젝트로 만든다.** 런타임 생성(`Instantiate`)만으로 끝내지 않는다.

이유: 에디터에 없으면 애니메이션 창으로 모션을 만들 수 없고, 크기·잡는 위치도
숫자로 타이핑할 수밖에 없다. 실제로 스윙 관련 인스펙터 값 35개를 눈으로 못 보고
맞추다 막혔다. 씬에 있으면 **끌어다 놓고 보면서** 맞춘다.

- 코드(`MountModel`)는 **찾아 쓰고, 없으면 만든다**. 찾은 경우에도 정렬 계산 전에
  모델 원본 자세·루트 크기 1·활성 상태로 되돌린다 (안 그러면 지난 실행 결과를
  다시 입력으로 읽어 실행마다 누적된다).
- 씬에서는 **작업할 무기 하나만 켜 둔다.** 넷이 다 켜져 있으면 손 안에서 겹친다.
- **소유권**: 리그 본체(`HandRig`)와 무기 안쪽 모델은 코드가, `HandL`·`HandR`·`Bow`
  는 애니메이션 클립이 갖는다. 클립에 무기 모델 키프레임을 넣으면 코드와 충돌해
  무기가 떨린다.
- 스윙 클립은 **정확히 1.00초**. 공속은 클립 배속으로 낸다.

## ★바(체력·수치) 규칙 (2026-07-29 사용자 확정)

**바는 스케일로 늘려 쓰지 않는다. 그 크기에 맞게 다시 만든다.**

작게 그린 텍스처를 크게 늘리면 모서리가 뭉개진다. 바를 키우면 뭉개짐도 같이 커진다.
"조금 흐린데?" 싶으면 스케일을 의심하기 전에 **원본 해상도부터 본다.**

- **둥근 바** — 표시되는 크기보다 텍셀이 촘촘하게 그린다. `FX.RoundedTex()` 는 512×192.
  밉맵을 켜서 멀어졌을 때 지글거리는 것도 막는다. 한 번 만들어 캐시하므로 커도 부담 없다.
- **각진 바** — 스프라이트를 **아예 쓰지 않는다** (`Image.sprite = null`).
  유니티가 1×1 흰 픽셀로 그려서 어떤 크기에서도 완벽하게 또렷하다.
  차징바가 이 방식이다 (둥근 스프라이트를 Sliced 로 늘리던 것을 걷어냄).
- **월드 공간 바는 화면 크기를 고정한다.** 원근 카메라에서 화면 크기는 (월드크기 ÷ 거리)
  이므로 **거리에 비례**시켜야 고정된다. 월드 크기를 고정하면 정반대로 동작한다
  (줌아웃할수록 화면에서 작아진다 — 실제로 그 버그가 있었다).

## ★이펙트 규칙 (2026-07-29 사용자 확정 — 절대 어기지 말 것)

**실제 동작을 따라가는 이펙트만 넣는다.**

- **몸이 실제로 하는 일**을 보여주는 것은 좋다 — 무는 순간 그 입 앞에서 튀는 먼지,
  찍는 순간 발밑에서 퍼지는 파동, 휘두른 궤적을 따라가는 잔상.
- **범위·영역·물리적 움직임과 안 맞는 장식은 넣지 않는다.** 몸은 가만히 있는데 터지는
  빛, 실제 타격 범위와 다른 크기의 원, 동작과 무관한 번쩍임 — 전부 금지.
- 판단 기준 한 줄: **"이 이펙트를 지우면 무슨 일이 일어났는지 못 알아보는가?"**
  못 알아보면 필요한 것이고, 그래도 알아보면 장식이다.
- 이펙트로 임팩트를 만들려 하지 말 것. **임팩트는 몸의 3막(예비·본동작·여운)에서 나온다**
  (`.claude/skills/motion-authoring`). 이펙트는 그 위에 얹는 것이지 대신하는 게 아니다.

## ★펫 모션은 「구부리기」가 주역이다 (2026-07-30 사용자 확정)

**강체 변형(크기·기울기·회전)만 쓰면 나무토막처럼 보인다. 살아있는 것은 휜다.**

`PetToon.shader` + `PetBend.hlsl` 에 버텍스 벤드가 있다 — `_BendF`(앞뒤) · `_BendS`(좌우) ·
`_Twist`. 이게 **있는데도 공격이 안 쓰고 있어서** 딱딱했다(장전·펀치만 물려 있었다).
지금은 방식 11개와 사망 연출이 전부 벤드를 쓴다.

- **부호:** `_BendF = -sw × k` → 예비에 말리고 본동작에 편다. `_BendS = sw × k` → 좌우 후림.
- **★`_BendSPivot` — 좌우 휨의 축.** 0=몸 중심(머리·꼬리가 반대로) · **1=머리 고정
  (꼬리가 앞까지 넘어온다)** · -1=꼬리 고정. 휩쓸기가 1을 쓴다.
- **★몸 전체 회전(yaw)은 안 쓴다** (사용자 "몸 전체를 회전시키는 모션은 지양").
  좌우 회전은 `PetUnit.Face` 의 소유이고, 회전은 실루엣을 거의 안 바꿔서 옆에서 보면
  아무 일도 안 난 것처럼 보인다. 좌우 동작은 전부 `_BendS` 가 낸다.
- **★죽으면 `PetMotion` 이 꺼진다.** 사망 연출의 벤드는 `PetUnit.PushDead()` 가 직접 쓴다.
  이때 **`_RefLen`·`_AxisX` 를 같이 안 넘기면 몸이 엉뚱한 데서 꺾인다**
  (`SetPropertyBlock` 은 블록을 통째로 갈아끼우므로 기본값 1/0 으로 떨어진다).

**사망 = 근육이 하는 일 그대로:** 고통에 말리고(+) → 넘어가는 순간 절정 → 힘이 빠지며
펴지고 살짝 지나쳐 늘어진다(−). **말림의 절정과 넘어지는 지점을 겹쳐야** "힘이 빠져서
쓰러진다" 로 읽힌다.

**★죽음 디졸브는 셰이더가 면을 실제로 지운다** (2026-07-30 사용자 — "메시 면이 지워지면서
지워지는 면의 경계를 빛나는 파티클로"). 전엔 `localScale` 로 몸을 줄이면서 주변에 파티클을
뿌렸다 — "얹혀 있다" 던 그것. 지금은 `clip()` 으로 면을 잘라내고 경계 띠가 발광하며,
입자는 **지금 지워지고 있는 높이**에서만 난다. **그림자·뎁스 패스도 같이 잘라야** 사라진
몸의 그림자가 안 남는다. 무채색은 휘도로 눌러 진짜로 색을 뺀다(`_Desat`) — 곱하기로는
원래 색이 비친다.

**모션 쇼케이스 `Assets/Scripts/MotionShowcase.cs`** (씬의 `PetSpawner` 에 붙어 있음):
`` ` `` 또는 `\` 로 켠다. 방식 11개를 한 줄로 세워 **동시에** 때리게 한다(시차를 주면 비교가
안 된다). ←→ 몸 바꾸기 · ↑↓ 크기 · Enter 죽음 연출 · Backspace 느리게.
★쇼케이스 펫은 **야생 + 시야 0** 으로 세운다 — 내 편으로 두면 "근처에 적이 없으면 전장으로
걸어간다" 규칙 때문에 전부 달려가 버린다. `PetUnit` 을 끄지 않는 이유는 죽음 연출이
`Update` 에서 돌기 때문이다.

## ★토큰 절약 규칙 (반드시 지킬 것)

Unity MCP 는 방심하면 토큰을 폭식한다. 다음을 기본값으로 삼는다.

1. **스크린샷 금지.** 사용자가 직접 화면을 보고 **말로** 알려준다. 이미지가 제일 비싸다.
   (명시적으로 요청받을 때만 예외)
2. **코드는 MCP 말고 파일로 직접 쓴다.** `.cs` 를 Write/Edit 로 고치면 유니티가 자동
   컴파일한다. MCP 로 코드를 주고받지 않는다.
3. **MCP 는 두 가지에만 쓴다** — ①씬에 오브젝트 배치 ②콘솔 **에러** 읽기.
4. **씬 계층 전체 덤프 금지.** 반드시 특정 오브젝트만 좁혀서 조회한다.
   본 씬(6km 섬)은 나무·풀이 수천 개라 한 번 읽으면 수만 토큰이 날아간다.
5. **프로토타입은 `Assets/Scenes/Sandbox.unity` 에서** 만든다. 본 씬에서 시작하지 않는다.
6. **콘솔은 에러만.** 경고·일반 로그는 읽지 않는다.
7. **수정은 몰아서 하고 확인은 한 번.** 한 줄 고칠 때마다 확인하지 않는다.
8. **★큰 데이터 파일은 문맥에 읽지 않는다 — 스크립트로 처리한다.**
   `Assets/World/` 가 특히 위험하다: `island_height.bin` **16.7MB**, `island_hm.png` 1.6MB,
   `continent.json` 106KB(≈3만 토큰), `_elev.txt`/`_biome.txt` 각 40KB+.
   필요하면 **이 파일들을 처리하는 코드를 짜서 유니티가 돌리게 하고, 결과 숫자만 받는다.**
   토큰을 먹는 건 "맵이 커서"가 아니라 "맵 데이터를 읽어서"다.
9. **환경 소품은 개별 GameObject 수천 개로 뿌리지 않는다** (NatureBuilder 처럼 배치·병합).
   계층이 비대해지면 그때부터 씬 조회가 진짜로 비싸진다. 현재 SampleScene 은 13개라 안전.

## ★길 규칙 (사용자 확정 — 절대 어기지 말 것)

**길은 반드시 불규칙하게 구불거려야 한다. 일자 길은 없다.**
그리고 *규칙적으로* 구불거려도 안 된다 — 사인파처럼 일정한 물결은 인공적으로 보인다.
`MapPlanImporter.IrregularMeander` 가 세 겹으로 보장한다:
 ① **비배음 3겹 노이즈** (주파수 1 : 2.3 : 5.7) — 배수 관계면 무늬가 반복돼 보인다
 ② **진폭 변조** — 진폭 자체를 다른 노이즈로 흔들어 *어떤 구간은 거의 곧고 어떤 구간은 크게 굽게*.
    이게 "불규칙"의 핵심
 ③ **불규칙 킥** — 무작위 *간격*으로 한 번씩 훅 꺾음(스위치백). 간격이 균등하면 그것도 규칙이 된다
폭도 노이즈로 흔든다. 길마다 씨앗이 달라 무늬가 겹치지 않는다.

## 도구 메뉴 (Tools/토이라기) — 딱 3개

도구가 많으면 뭘 눌러야 할지 헷갈린다. `Assets/Editor/WorldBuilder.cs` 하나에 다 있다.

- **① 월드 전부 다시 짓기** — 지우고 → 마커 → 길 → 나무 → 풀 을 **올바른 순서로 한 번에**.
  (길을 먼저 칠해야 나무·풀이 길을 피한다. 순서를 사람이 기억할 필요 없게 묶었다)
- **② 월드 전부 지우기** — 마커·길·나무·풀 원상복구. **지형 높이는 안 건드린다.**
  길은 알파맵 덧칠이라, 흙 레이어를 0 으로 밀고 잔디로 되돌려야 지워진다.
- **③ MCP 카테고리 최소화** — 44개 중 8개만 (토큰 절약).

원본 계획: `Assets/World/mapplan.txt` (마커 99, 길 162구간, 격자 81×81).
길 폭은 계획 사양 그대로 — 대로 6m / 샛길 3m / 오솔길 1.5m / 브론토 순환로 60m
(단 **지형 실측 크기에 비례 환산**해서 적용).

## 현재 상태 (2026-07-29)

**한 줄:** 키운 펫을 **무기로 던져 그 자리에 무리로 소환**하고, 떼 대 떼로 싸운다.
목표 규모는 **50대50**. (탑승 ✗ 폐기 · 조립 크래프팅 ✗ → 대규모 소환 전투 ⭕)

### ★조작 (2026-07-29 기준)

| 키 | 동작 |
|---|---|
| 1~0 | 핫바 10칸 (**1·2·3 이 무기**, 나머지는 아이템) |
| 좌클릭 | 무기 기본 공격 — **누르고 있으면 차징 3단**(피해·범위 ↑, 발은 느려짐) |
| 우클릭 | 카메라 회전(드래그) · **조준 중이면 투척 취소** |
| 휠 | 카메라 줌 (지도 위에서는 지도 확대축소) |
| **Q·E·R** | **펫 대규모 투척** — Q=1번칸·E=2번칸·R=3번칸 펫. 누르면 궤적·착탄 반경 미리보기, 놓으면 발사 |
| Space | 구르기 (잔상 대시) |
| M | 지도 (회전·줌) · Tab 메뉴 |

- **된 것:** 지형·길·환경·**물 판정과 수영** / 무기 평타·**좌클릭 차징** /
  레벨·**전투력 표시** / 인벤·제작대·건축 / 채집·리스폰 /
  Tab 메뉴·**핫바 10칸**·**지도 회전·줌**·HUD / **펫 전투 행동(찾기→접근→타격)** /
  **어그로 3단** / **투척 소환** / **야생 어그로 시 증식** / **전투 종료 자동 회수** /
  **펫별 쿨타임** / **펫 레벨을 종족이 공유**(하나 잡으면 같은 종 전부 성장, 지수 곡선) /
  **활 조준을 애니메이션 클립으로**(시위·화살까지 씬에 실존) / **둥지·알 빛줄기**
- **안 된 것:** 조립 3층 · 설계도 · 도감 · 지형 전술 ·
  **부화기 웨이브 진군**(펫 행동 삭제 때 끊김, 아래 참고)
- **★폐기 (2026-07-29 사용자):** **펫·야생의 레벨 — 없앤다.**
  펫은 **종 고유의 스탯**을 갖고 그대로 간다. 레벨로 크지 않는다.
  강해지는 길은 셋 — **전리품(펫당 1개 장착) · 칭호 · 본캐릭터의 노드.**
  (설계 정본 3층 구조의 **3층(악세)** 자리와 같다. 새로 뒤엎는 게 아니다)
  **야생도 같이 없앤다** — 난이도는 "그 지역에 무엇이 몇 마리 나오나" 로만 낸다.
  아래 「거리 등급 → 지역 등급」과 같은 방향이고, 내 펫만 고정 스탯인데 적만
  레벨이 붙는 어긋남도 사라진다.
  → 걷어낼 것: `PetUnit` 의 level·xp·LevelUp·ApplyLevels·ApplyGrowth·SetWildLevel·
    points 계열 / `PetBox` 의 level·xp·GainXP·GainXPAll / 머리 위 레벨 표시 ·
    Tab 펫 스탯창 / `PetSpawner.tierPerMeters`.
  **★`PlayerLevel` 은 남는다** — 캐릭터는 계속 레벨이 오르고, 그 노드가 펫을 강하게 한다.
- **★폐기 (2026-07-29 사용자):** **편성 예산(인구수 10) — 없앤다.** 부대를 짤 때
  "인구수 예산 안에서 고른다" 는 층을 두지 않는다. 핫바 3칸에 무엇을 넣든 제한 없다.
  (헷갈리지 말 것 — **투척 마릿수 `throwBudget` 과 야생 증식 `packBudget` 은 그대로다.**
  이름은 비슷하지만 "몇 마리 나오나" 이지 "무엇을 데려갈 수 있나" 가 아니다)

### ★대규모 소환 = 나중에 디펜스의 뼈대다 (사용자 못박음)

이 시스템은 **거점 방어(웨이브 디펜스)에 그대로 재활용된다.** 지금 짤 때부터
"플레이어 중심"으로 굳히지 말 것. 갈라지는 지점은 셋뿐이다:

1. **전투 종료 판정** (`PetCommand.NearMine`) — 지금은 *플레이어·분신 주변*에 야생이
   없으면 종료다. 디펜스에선 **거점(부화기) 주변**이 기준이어야 한다.
   → 기준점을 "내 편 앵커 목록"으로 넓히면 양쪽 다 쓸 수 있다. 플레이어를 하드코딩하지 말 것.
2. **복귀 목적지** (`PetUnit.ReturnStep`) — 지금은 무조건 플레이어에게 걸어가 흡수된다.
   디펜스에선 **거점으로 돌아가 자리를 지키는** 편이 맞다.
3. **야생의 행선지** — 지금은 리쉬(`homePos`)로 돌아간다. 디펜스에선 **거점으로 진군**해야
   한다(예전 `forceTarget` 자리). 리쉬를 "무조건 귀환"으로 굳히지 말 것.

**그대로 쓰이는 것 — 건드리지 말 것:**
- **인구수 예산 ÷ 등급** (`PetUnit.CountFor`) — 웨이브 규모 조절에 그대로 맞는다.
  작은 놈은 떼로, 큰 놈은 몇 마리만. 숫자 하나로 난이도가 움직인다.
- **등장 연출** (`LaunchTo`/`FlyStep`) — 웅크림 → 포물선 → 착지 텀, 개체마다 시차.
  웨이브 등장도 이걸 쓴다.
- **체력바는 전투 중에만** — 50대 50에서 바 100개를 매 프레임 갱신하면 프레임이 무너진다.
- **목표 탐색 시점 분산** (개체마다 어긋난 0.5초 주기).

### 세계 스케일 — 지형 6km, 나머지는 1/10

24km 타일 지형은 **폐기**했다(`archive-24km` 브랜치에 보관). 대신 지형은 6km 한 장
(`Terrain_0_0`)으로 두고 **지형 외 전부를 1/10 로 줄였다**. 배율은 `WorldScale.K` 하나.
**미터 단위 상수를 새로 쓸 때는 반드시 `* WorldScale.K` 를 곱한다.**
안 곱하면 "화살이 4m 앞 허공에서 나가는" 류의 버그가 난다.

### ★캐릭터 키는 **1.0m** 다 — 재보고 쓸 것 (2026-07-30 실측)

여기 오래 **"0.42m" 라고 적혀 있었는데 틀린 값이다.** 실제로 재보니 1.000m 였다.
그 숫자를 믿고 펫 크기를 맞췄다가 S 가 캐릭터의 63% 로 나와 "왜 이렇게 작지" 가 됐다.

**크기·거리를 만질 때는 문서의 숫자를 믿지 말고 씬에서 직접 재라.** 렌더러 바운즈를
합쳐서 `size.y` 를 보면 된다 (`unity_execute_code` 로 한 줄이면 나온다).

### 손·무기 모션 = 애니메이션 클립

`HandRig`(플레이어의 *형제* 오브젝트) 밑에 손·무기가 **씬에 실존**하고, 모션은
`Assets/Animation/` 의 클립 4개(Aim·Carry·Swing_Horizontal·Swing_Vertical)가 그린다.
소유권 규칙은 위 「손·무기는 반드시 씬에 실존시킨다」 참고.

**활 (2026-07-29):** 활은 **왼손(`HandL`) 밑**에 있고, 시위·화살도 씬에 실존한다
(끝점 세 오브젝트 + 화살은 `Nock` 밑 — 시위를 당기면 화살이 저절로 따라온다).
조준 자세는 코드가 아니라 **Aim 클립**이 그린다. 쏘는 순간만 짧게 섞어 자세가 안 끊기게 한다.

## ★밸런스 계산법 (2026-07-28 확립 — 앞으로 이 기준으로 잰다)

> **★아래 표의 펫 수치는 낡았다 (2026-07-29).** 인구수(1/3/7/14)·스탯·공격 방식이
> 전부 바뀌었고, 광역은 상한 없이 부채꼴 면적으로 나간다. **계산으로 잡지 말고
> `StressTest` 의 F7 로 재라** — 이 날 계산이 네 번 틀렸다. 계산법(면적 × 동시 타격)은
> 여전히 옳지만, 숫자는 실측으로 갱신해야 한다.

**초당 피해만 보면 틀린다.** 근접은 부채꼴 안의 적을 *전부* 때리므로 1대1 수치가
실제 화력이 아니다. 실제로 이것 때문에 한 번 잘못 조정했다.

```
유효 화력 = (한 대 피해 ÷ 공격 간격) × 동시 타격 수
동시 타격 수 ≈ 부채꼴 면적 ÷ 적 한 마리가 차지하는 면적
             (적 간격은 Separate 가 (내 몸+상대 몸)×0.42 로 벌린다)
```
여기에 **사거리(안전한가)** 와 **체력(버티나)** 을 같이 본다. 원거리는 단일 타격이라
유효 화력이 낮은 대신 안 맞는다 — 그 값을 화력으로 상계한다.

**기준선: 플레이어의 유효 화력 ≈ 펫 3마리(약 86).**
펫 부대 10마리(약 286)의 30% — 거들되 대신하지 않는 몫이다.

| | 한 대 | 간격 | 각도×사거리 | 동시 | 유효화력 | 성격 |
|---|---|---|---|---|---|---|
| 칼 | 13 | 0.38 | 104°×0.50 | ~2.5 | ~86 | 빠르고 단일에 강함 |
| 도끼 | 9 | 0.50 | 176°×0.63 | ~5 | ~86 | 떼를 쓸어친다 |
| 곡괭이 | 18 | 0.72 | 72°×0.47 | ~1.8 | ~45 | 채집용, 전투는 어정쩡 |
| 활 | 18 | 0.45 | 단일·12m | 1 | ~40 | 안전한 대신 떼엔 약함 |
| 펫 1마리 | str×3.5 | 1.1 | 단일 | 1 | ~29 | — |

- **체력:** 플레이어 450 · 야생 M 150. 야생 3마리가 붙으면 5초, 10마리면 1.6초 버틴다.
  → 떼 한복판은 죽는 자리다. 근접은 치고 빠져야 하고, 그게 구르기(Space)의 존재 이유.
- **영역은 무기별로 다르다** (`PlayerGather` 의 무기별 사거리·각도 배수).
  여태 셋이 같은 부채꼴이라 무기를 바꿀 이유가 숫자뿐이었다 — 영역이 성격을 가른다.
- **★위 표는 차징을 안 넣은 값이다 (2026-07-29).** 좌클릭을 물고 있으면 2단 ×1.9 /
  3단 ×3.2 배(범위도 ×1.25 / ×1.6)로 나간다 — 대신 차징 동안 발이 느려진다(`chargeMoveSlow`).
  **플레이어 화력을 다시 잴 때는 차징 배수를 반드시 같이 본다.**

## ★검증된 것 (다시 묻지 말 것)

- **50대50 은 프레임이 남는다 (2026-07-29 실측).** `StressTest.cs` 로 100마리를 실제로
  붙여 봤고 렉이 전혀 없었다. **규모 때문에 설계를 뒤집을 위험은 없다.**
  → 웨이브 규모를 정할 때 "무거울까 봐" 줄이지 말 것. 재보고 정한다 (F9 를 누를수록 쌓인다).
- **★`Separate()` 의 O(n²) 는 이미 고쳐졌다 (2026-07-30 확인).** 여기 오래 "매 프레임
  전체를 훑는다" 고 적혀 있었는데, 지금은 `BuildCells()` 로 **주변 3×3 칸만** 본다.
  `FindTarget`·`Strike`·`Splash`·`ConeTargets` 도 전부 격자를 쓴다.
  → **프레임이 안 나올 때 여기를 의심하지 말 것.** 남은 후보는 다른 데 있다
  (렌더링·드로콜·모델 폴리곤·이펙트 수). **범인은 F1~F4 로 껐다 켜서 찾는다.**

## ★정체성·상성 (2026-07-29 — 정본은 `docs/superpowers/specs/2026-07-29-identity-counters-design.md`)

- **★상성표를 만들지 않는다.** 크기4 × 사거리3 × 역할4 = 48칸은 만들 수도 읽을 수도 없다.
  **법칙 3개로 저절로 생기게 한다** — ①넓게 때리는 놈은 뭉친 떼에 강함 ②멀리 때리는 놈은
  느린 놈에 강함 ③한 방이 무거운 놈은 큰 놈에 강함. 스타2에도 상성표는 없다.
- **지금 법칙 ①이 꺼져 있다** — 펫이 전부 단일 타격이라 물량이 벌을 안 받는다.
  **광역 타격이 상성의 시작점**이고, 마릿수 격차와 짝이다.
- **크기는 "몇 마리 · 얼마나 커 보이나" 만 정한다.** 지금은 크기가 역할까지 정해서
  (`RoleOf(species, tier)`) 크기가 정체성이 아니라 강함 순서로 읽힌다.
- **★거리 등급 폐기 → 지역 등급** (사용자 지시). `tierPerMeters` 를 바이옴 테이블로.

## ★다음 할 일 (2026-07-29 회사 작업 끝 시점)

**바로 이어서 할 것 — F5·F7·F6 을 다시 재는 것.** 마지막에 버그 둘을 고쳤는데(뒷줄이
전장으로 안 감 · 원거리가 포위함) **그 뒤로 아직 안 쟀다.** 게다가 그때까지는 레벨업이
판 도중에 풀피를 뿌리고 있었다(위 실험대 항목). 앞서 나온 숫자는 전부 못 믿는다.

**지금 재도 안 아깝다** — 실험대는 양쪽을 1레벨로 맞추는데 1레벨 보정이 정확히 1배라,
**1레벨 스탯 = 종 고유 기본 스탯**이다. 레벨을 걷어낸 뒤에도 지금 잰 값이 그대로 유효하다.

1. **상성 삼각형 재측정** — 유니티 실행 → F5 · F7 · F6 (실험대는 씬의 `PetSpawner` 에 붙어 있다)
   ```
   티라노 > 자글이   (법칙① 넓게 때리는 놈은 뭉친 떼에 강하다)  — 확인됨
   불호랑이 > 티라노 (법칙② 멀리 때리는 놈은 느린 놈에 강하다)  — 방향만 확인
   자글이 > 불호랑이 (법칙③ 빠른 떼가 원거리를 덮친다)          — 방향만 확인
   ```
   목표는 **이긴 쪽 체력 30~50%**. 80% 넘으면 너무 세고, 10% 밑이면 상성이 없는 셈.

   **★2026-07-29 밤 — 삼각형이 닫혔다 (가위바위보 성립):**
   ```
   F5 ①  티라노  승 — 19.5초 ·  8마리 남고 62%  → 적당 ★
   F7 ②  불호랑이 승 — 42.8초 · 38마리 남고 76% → 적당 ★
   F6 ③  자글이  승 — 16.2초 · 86마리 남고 57%  → 적당 ★
   ```
   **상성표 없이 법칙 3개 + 속도표만으로 나왔다.** 여기까지 온 경로(이 순서가 중요하다):
   1. 경험치 잠금 — 그전 값은 전부 레벨업 풀피에 오염돼 있었다
   2. 접근 판정 수정 — 티라노가 사거리 밖에 서서 구경만 하던 버그 (아래 항목)
   3. 사거리 7→14 · 쏘기 피해 0.85→1.25 — **둘 다 효과가 작았다**(②변 29%→19%)
   4. **카이팅** — 이게 결정타. ②변이 한 번에 뒤집혔다

   ★**③변이 오히려 팽팽해진 것**(74%→57%)이 설계가 맞았다는 증거다. 불호랑이가
   자글이한테서 도망치려다 못 도망치고 사격만 놓친다 — **같은 카이팅이 한쪽에선
   무기가 되고 한쪽에선 짐이 된다.** 법칙②와 ③이 한 메커니즘에서 나온다.

   ★**남은 흠: 세 변이 다 목표(30~50%)보다 높다** (62/76/57). 특히 ②의 76% 는
   압승 문턱(80%) 코앞이라, 상대를 맞춰 가면 거의 자동으로 이기는 셈이다.
   → 조일 때는 **`kiteCooldown` 하나만** 만져라. 카이팅은 자글이 상대로는 어차피
   안 통하므로 **②만 움직이고 ③은 거의 안 건드린다.** 쏘기 피해(`PatternDmg`)를
   만지면 ②③이 같이 흔들려 두 변을 동시에 쫓게 된다.
2. **펫·야생 레벨 걷어내기** (2026-07-29 사용자 확정 — 위 「폐기」 참고).
   측정이 끝난 뒤에 한다. 펫은 종 고유 스탯으로 고정되고, 강해지는 건
   전리품(1개)·칭호·본캐 노드가 맡는다. `PlayerLevel` 은 남긴다.
3. **재질 축 켜기** — 원거리를 '호랑이라는 종' 이 아니라 **유리 재질**로 옮긴다.
   설계 3층의 2층이고, 모델을 새로 안 뽑아도 같은 종이 쇠/유리/천으로 갈린다.
   ("스몰에도 다양한, M 에도 다양한" 이라는 사용자 요구가 이 층이다)
4. **부화기 웨이브 진군 복구** — 위 「디펜스의 뼈대」 3번. 이게 되면 디펜스가 열린다.
5. 무기별 투척 궤적 분화 (지금은 전부 단순 던지기 하나 — 사용자: "차차 추가")
6. **플레이어 화력 재점검** — 표의 "초당 100" 은 차징(최대 3.2배) 이전 값이고,
   펫 스탯도 2026-07-29 에 전부 바뀌었다. 기준선을 다시 잡아야 한다.

### 실험대 `Assets/Scripts/StressTest.cs` (측정 전용 · 씬의 PetSpawner 에 붙어 있음)

| 키 | 하는 일 |
|---|---|
| **F5 · F7 · F6** | **상성 삼각형 세 변**(①②③). 연달아 누르면 한 바퀴 돈다 |
| F8 | 오른쪽 편성만 야생으로 — 플레이어 혼자 몇 초에 잡나 |
| F9 | N대N 무작위 (프레임 측정용, 누를수록 쌓임) |
| F10 · F11 | 전부 지우기(평소 게임 복귀) · 측정 초기화 |
| F1~F4 | 체력바 / 테두리 / 피해숫자 끄기 — **범인 찾기용** |

- **예산**만 정하면 양쪽 마릿수가 자동 (예산 ÷ 등급별 인구수). 공평한 판이 저절로 만들어진다.
- 실험 중엔 야생 스포너를 멈추고 판을 비운다 — 안 그러면 한쪽에만 지원군이 붙어 오염된다.
- **★실험 중엔 경험치가 잠긴다** (2026-07-29 사용자 "레벨업하면서 다 풀피가 되버리니까
  측정이 안돼네"). 야생이 죽을 때마다 종이 레벨업하고, 그때 `ApplyLevels` 의 `hp = maxHp`
  가 **살아 있는 같은 종을 전부 풀피로 되돌렸다.** maxHp 도 같이 커져 체력%가 100을 넘겼다.
  → 막는 자리는 `PetBox.GainXP` 와 `PlayerLevel.Gain` **두 깔때기 입구**다. 부르는 쪽을
  하나씩 쫓지 않아도 모든 경로가 닫힌다. F10 을 누르면 풀린다.
  (펫 레벨을 걷어내고 나면 앞의 절반은 저절로 없어지고, `PlayerLevel` 쪽만 남는다)
- **★짐작으로 고치지 말 것.** 이 날 하루에만 짐작이 네 번 틀렸다(천장 400·티라노 승패·
  렉의 범인·상성 원인). 껐다 켜서 재고, 숫자를 보고 고쳤다.

### ★로스터 9마리 (2026-07-29 사용자 확정)

| 펫 | 크기 | 방식 | 역할 | 마릿수(예산140) | 이속 | 간격 |
|---|---|---|---|---|---|---|
| 늑구 | S | 물기 | 암살자 | 140 | 6.60 | 0.58초 |
| 꼭꼬 | S | 쏘기 | 포수 | 140 | 3.30 | 1.29초 |
| 랍또 | M | 돌진 | 암살자 | 46 | 5.85 | 0.58초 |
| 호동 | M | 물기 | 돌격병 | 46 | 4.49 | 1.83초 |
| 딜롭 | M | 쏘기 | 포수 | 46 | 2.93 | 1.29초 |
| 트리통 | L | 돌진 | 돌격병 | 20 | 3.80 | 1.83초 |
| 케몽 | L | 쏘기 | 포수 | 20 | 2.48 | 1.29초 |
| 티라 | XL | 물기 | 거인 | 10 | 1.38 | 2.20초 |
| 븐토 | XL | 휩쓸기 | 방패 | 10 | 1.75 | 1.47초 |

크기마다 근접·원거리가 짝을 이루고, **같은 크기 안에서 방식이 전부 다르다** (겹치는 조합 없음).
원거리는 꼭꼬 S · 딜롭 M · 케몽 L 로 크기별 셋 — "S 원거리·XL 원거리는 어떻게 되나" 라는
물음이 로스터로 저절로 검증된다.

★**늑구가 S 라서 지금까지 잰 삼각형이 그대로 살아 있다.** "자글이" 는 늑대를 S 로 강제한
가짜였는데 이제 진짜 종이 됐다. ①티라>늑구 ②딜롭>티라 ③늑구>딜롭 이 실제 종 대결이 된다.
(딜롭은 M·포수·쏘기라 "불호랑이" 와 수치가 같다)

★**기존 5종의 모델 파일을 새 이름으로 다시 임포트하지 말 것.** GUID 가 바뀌어 씬의
프리팹·머티리얼 연결이 전부 끊어진다. `koreanName` 만 바꾸고 `species` ID(`chibi_*`)는
그대로 둔다 — 보관함 저장이 그 ID 를 쓰고, **분류는 이제 이름이 아니라 드롭다운이 한다.**

### ★★숫자가 이상하면 **측정 조건을 먼저 의심하라** (2026-07-30 — 하루에 세 번 당했다)

세 번 다 "밸런스가 이상하다" 로 보였지만 **실은 측정이 이상했다.** 밸런스를 만지기 전에
이 셋을 먼저 확인한다:

| 오염원 | 증상 | 막는 자리 |
|---|---|---|
| **레벨업** | 판 도중 살아있는 같은 종이 전부 풀피가 됐다 | `PetUnit.DebugNoXP` (경험치 잠금) |
| **리쉬** | 야생 편이 26m 넘으면 표적을 버리고 스폰 자리로 걸어갔다. **빠른 종이 제일 자주** — 사도사가 1승 11패였던 정체 | 측정판에서 `leashRange = 99999` |
| **배속** | 100배에서 한 프레임에 1.67초가 흘러 사도사가 **프레임당 13.5m**(실험장 폭 15m)를 뛴다. 서로 지나쳐 무승부 17개 | `StressTest.leagueMaxSpeed`(10). 결과 파일 헤더에 배속을 적는다 |

★**판별법: 결과가 「특정 성질」을 따라 줄 세워지면 측정 오염이다.** 배속 사고는
이속 순서와 붕괴 순서가 정확히 겹쳤다(사도사 8.1→0승 … 딜롭 2.7→무패). 밸런스 문제는
그렇게 깔끔하게 한 축을 따라가지 않는다.

### ★★면적은 「각도 × 팔²」 이다 — 각도만 보면 반드시 틀린다 (2026-07-30)

팔 길이가 면적에 **제곱**으로 들어간다. 이 하나를 놓쳐서 같은 형태의 오진을 네 번 했다:

- **"좁고 길다"(들이받기 40°×1.8)가 실제로는 제일 넓었다** — 면적 130, 물기(55°×1.0)의 2.4배
- **"사방으로 넓다"(내려찍기 360°×0.8)가 실제로는 제일 좁았다** — 각도로 번 것을 팔로 다 잃었다
- **모델이 홀쭉하면 팔이 짧아져 전투력이 1/4** (랍또 bodyR 0.98 ↔ 호동 1.33 → 면적 46 ↔ 185)
- **작은 등급은 광역을 뚫고 건너는 데 오래 걸린다** (S 팔 0.54 ↔ 븐토 4.19, 7.8배)

★**팔 길이의 기준은 `bodyR`(실측 몸)이 아니라 「등급 목표 크기」다** (`PetUnit.TierArm`).
`bodyR` 로 하면 **조각칼이 밸런스를 바꾼다** — 홀쭉하게 만들면 그 펫이 조용히 약해진다.
비율은 밀림 간격과 `+ bodyR + t.bodyR`(표면 기준 사거리)에 그대로 남으니 죽는 게 아니다.
**비율은 "몸이 어디까지 차지하나", 등급은 "팔이 얼마나 뻗나"** 를 정한다.

### ★남은 구조 문제 — 등급이 클수록 강하다 (2026-07-30, 미해결)

```
XL 8.7승  ·  M 6.0  ·  L 5.7  ·  S 3.3      (78판 평균, 예산 140 동일)
```
총 체력은 등급마다 약 1만으로 맞췄고 총 화력은 S 가 XL 의 10배인데도 S 가 약하다.
**면적이 등급 사이에서 38배 벌어지기 때문이다** (등급키 1:2:3.8:6.2 → 제곱 1:4:14.4:38.4).
마릿수는 14배만 보상한다(140:46:20:10). 그 어긋남이 2.6배 승수 격차다.

→ 선택지: ①팔을 등급키에 덜 비례시킨다(제곱근 쪽) ②인구수를 면적에 맞춘다(1/4/14/38)
③작은 등급을 더 올린다. **①이 근본이지만 거인의 팔이 몸보다 짧아 보이는 대가가 있다.**

### ★성능 — 범인은 펫이 아니라 환경이었다 (2026-07-30)

`Batches 7574` · `Shadow casters 8607` 인데 펫 100마리는 300 배치뿐이었다. 실제 원인:

- **지형 트리 10만 그루의 머티리얼에 GPU 인스턴싱이 꺼져 있었다** → 한 그루당 드로콜 하나.
  `SetPass 506` 에 `Batches 13090` 이 그 증거(같은 셰이더인데 따로 그린다).
  ★대부분이 `.glb` 에 박힌 머티리얼이라 **에디터에서 켜 두면 재임포트에 날아간다.**
  glTF 임포터는 리맵도 안 되므로 `TerrainInstancing.cs` 가 **시작할 때 켠다.**
- **그림자 거리 320m 가 트리 거리 160m 의 2배** — 안 보이는 트리가 그림자를 드리웠다 → 100
- 캐스케이드 4→2 · MSAA 4→2 · basemapDistance 20000→300 · pixelError 5→12
- **새 펫 8종이 20만~52만 삼각형** (기존 5종은 30,000) → 전부 30,000 으로 감축.
  사도사 140마리가 7,330만 → 420만 삼각형

★**`Separate()` 의 O(n²) 를 의심하지 말 것 — 이미 격자로 고쳐져 있다.**

### ★리그전으로 삼각형이 실제 종으로 닫혔다 (2026-07-30, 45판)

```
늑구(S 물기) > 딜롭(M 쏘기)  12.6초 48%   법칙③ 빠른 떼가 원거리를 덮친다
딜롭(M 쏘기) > 티라(XL 물기) 45.1초 25%   법칙② 멀리 때리는 놈이 느린 놈을 잡는다
티라(XL 물기) > 늑구(S 물기)  9.0초 92%   법칙① 넓게 때리는 놈이 뭉친 떼를 쓴다
```

**여기까지 온 결정타는 「크기별 이속 폭」이었다.** 역할을 걷어내니 티라가 거인 이속(×0.55)을
잃고 물기(×1.3)를 받아 1.375 → 3.25 로 빨라졌고, **원거리 전부보다 빨라져** 카이팅이
구조적으로 불가능해졌다(티라 9-0-0). 크기 이속 폭을 1.8배 → **3.5배**(4.6/3.6/2.6/1.3)로
벌리자 딜롭이 티라를 이겼다. **화력은 안 건드렸다** — 티라가 센 건 맞고, *빨랐던* 게 문제였다.

★**밸런스를 볼 때는 「이속 순위표」를 먼저 그려라.** 법칙②③이 둘 다 이속에서 나오므로,
`늑구 > 딜롭 > 티라` 순서가 깨지면 어떤 숫자를 만져도 삼각형이 안 닫힌다.

### ★원거리는 부채꼴을 안 쓴다 — 광역 원거리는 코드가 필요하다 (2026-07-30)

`PetUnit.Strike()` 는 `IsRanged` 면 **투사체를 날리고 겨눈 한 놈만** 맞힌다. 즉
**원거리의 `PatternAngle` 은 아무 효과가 없다.** 흩뿌리기(60° 산탄)가 1-0-8 로 바닥인 것이
그 결과다 — 넓게 뿌리는 대가로 피해를 깎아뒀는데 넓게 뿌리질 못한다(피해를 2배 올려도 그대로였다).

→ 흩뿌리기·터뜨리기·꿰뚫기는 **투사체 쪽을 고쳐야** 산다. 숫자로는 못 고친다.

### ★한 방에 죽으면 물량은 무의미하다 — 오버킬 문턱 (2026-07-30)

늑구(S)가 근접 다섯 판을 **전부 잔여 0%** 로 졌고 티라전은 9초에 92% 남기고 전멸했다.
원인은 마릿수도 화력도 아니라 **오버킬**이었다 — 체력 30 에 티라 한 대가 66.5 라
**한 대에 두 마리 몫이 버려진다.** → S 체력을 70 으로 올려 한 방에는 안 죽게 했다.
**"몇 대에 죽는가" 라는 문턱이 물량의 가치를 결정한다.** 힘·체력을 볼 때 이 비를 같이 봐라.

### ★역할 폐기 — 축은 「크기 × 방식」 둘뿐이다 (2026-07-29 사용자 "합치자")

**역할(암살자·돌격병·방패·거인·포수)을 없앴다.** 역할과 방식이 **둘 다 종 이름에서
파생**돼 늘 같이 움직였고, 사거리는 아예 양쪽에 곱해지고 있었다
(트리케라 = 돌격병1.25 × 돌진1.8 = **2.25배** — 4전 전승한 이유의 일부).
축을 하나로 합치면 그런 이중 적용이 **구조적으로 불가능**해진다.

```
크기 등급 → 크기 · 힘 · 체력 · 마릿수 · 기본 공속(TierPeriod) · 기본 이속
방식     → 각도 · 팔 길이 · 한 대 피해 · 공속 배수 · 이속 배수
종별 기울임 → 공속 · 이속 · 사거리 · 각도 · 피해 (전부 1이 기준)
```

★**"굼뜬 거인" 은 이제 크기가 만든다.** 크기가 기본 템포를 주고 방식이 배수로 기울인다.
그래서 같은 '물기' 라도 늑구(S)는 촐싹대고 티라(XL)는 묵직하다 — 네가 확정한
**"넓은 건 방식이 아니라 몸이다"** 와 같은 원리다.

**방식 11개** (넓을수록 약하고, 무거울수록 느리다):

| 근접 | 각도 | 팔 | 피해 | 공속 | 이속 |
|---|---|---|---|---|---|
| 할퀴기 | 70° | 0.9 | 0.6 | **2.4** | 1.5 |
| 물기 | 55° | 1.0 | 1.0 | 1.5 | 1.3 |
| 후려치기 | 120° | 1.1 | 0.9 | 0.8 | 1.1 |
| 휩쓸기 | 200° | 1.25 | 0.5 | 0.9 | 0.9 |
| 내려찍기 | 360° | 0.8 | 0.55 | 0.7 | 0.8 |
| 들이받기 | 40° | 1.8 | **1.6** | 0.6 | 1.2 |
| 짓밟기 | 90° | 0.6 | **2.4** | **0.4** | 0.7 |

| 원거리 | 각도 | 팔 | 피해 | 공속 | 이속 |
|---|---|---|---|---|---|
| 연사 | 12° | ×0.64 | 0.5 | 2.2 | 0.8 |
| 쏘기 | 12° | ×1.0 | 1.25 | 0.85 | 0.75 |
| 저격 | 8° | ×1.29 | 2.0 | 0.5 | 0.6 |
| 흩뿌리기 | 60° | ×0.57 | 0.4 | 1.0 | 0.85 |

(원거리 팔 길이는 `shootReach`(14) 배수)

★**흩뿌리기만 카이팅을 안 한다** (`KitingPattern`). 가까울수록 강한 산탄이라 도망가면
정체성을 잃는다 — **원거리끼리도 상성이 생기는 지점**이다.
★**원거리가 느린 것이 법칙③의 근거다.** `PatternMoveSpeed` 를 1로 올리면 아무도 원거리를 못 잡는다.
★**`Pattern` enum 순서를 바꾸지 말 것** — 씬에 정수로 저장된다. 새 방식은 반드시 뒤에 붙인다.

### ★펫 분류는 인스펙터 데이터다 — 코드에 이름을 넣지 말 것 (2026-07-29)

**펫 추가 = 모델 넣고 `PetSpawner.entries` 에 줄 하나 + 드롭다운 고르기.** 코드는 안 건드린다.

전엔 `PatternOf`/`RoleOf` 가 종 이름 문자열로 분기했다(`s.Contains("tiger")`). 두 가지가 나빴다:
①펫 하나 추가에 두 함수를 고쳐야 했고 ②**이름을 바꾸면 에러 없이 조용히 기본값으로 넘어갔다**
("늑구" 에는 `wolf` 가 없다). 지금은 Entry 의 `방식`·`역할` 드롭다운이 이긴다.
`자동` 을 고르면 예전처럼 이름에서 추론한다 — 기존 5종 호환용이지 새 펫이 쓸 값이 아니다.

**종별 기울임 배수 5개** (전부 1이 기준): 공속 · 이속 · 사거리 · **부채꼴 각도** · **한 대 피해**.
★**방식을 종마다 새로 만들지 말 것.** 방식이 원형(영역 모양 + 한 대 피해)을 주고 종은
배수로 기울이기만 한다. 종마다 방식을 새로 파면 축이 무너져 밸런스를 못 잡는다.

**호랑이의 '임시 원거리' 는 걷었다** — 원거리 담당이 없어서 빌려뒀던 자리인데, 진짜
원거리 펫(꼭꼬·딜롭·케몽)이 생겨서 돌려줬다. 호동은 이제 M 기본값(물기 + 돌격병)이라
늑구(물기 + 암살자)와 역할이 갈린다.

### ★카이팅 = 쿨타임 있는 백스텝 (2026-07-29 사용자 기획)

**속도표가 이미 삼각형을 담고 있었는데 쓰는 행동이 없었다.** 역할 배수까지 곱한 실제 이속:

```
자글이  S  4.4 × 1.5 (암살자) = 6.6 m/s
불호랑이 M 3.9 × 0.75(포수)   = 2.93
티라노  XL 2.5 × 0.55(거인)   = 1.375
```
불호랑이는 티라노보다 **2.1배 빠르고** 자글이보다 **2.25배 느리다.** 물러날 줄만 알면
**②변은 뒤집히고 ③변은 안 건드려진다** — 숫자를 따로 안 맞춰도 상성이 저절로 생긴다.
(★이속을 볼 때는 반드시 **역할 배수를 곱한 값**으로 봐라. 기본값만 보면 티라노가
2.5 로 보여서 "차이 없다" 는 엉뚱한 결론이 난다 — 실제로 한 번 그렇게 틀렸다)

**★한계가 반드시 있어야 한다.** 티라노는 영영 못 따라잡으므로 무제한 카이팅이면
②변이 뒤집히는 게 아니라 **반대로 깨진다**(티라노가 이길 방법이 없어짐). `RoleOf` 의
"도망칠 수 있으면 아무도 못 잡는다" 가 이 얘기다. → 한 번 물러날 거리를 예산으로 묶고,
다 쓰면 쿨이 돌 때까지 제자리에서 버틴다. **조절 손잡이는 `kiteCooldown` 하나.**

**물러나는 동안은 못 쏜다** — 「쏘는 놈은 선 자리에서 쏘는 게 전부다」를 지키는 자리이자,
"뒤로 걸으며 때리는 게 언제나 정답" 이 되는 걸 막는 대가다.

### ★「멈추는 거리」와 「때리는 거리」를 따로 계산하지 말 것 (2026-07-29)

**증상:** 티라노(XL)가 원거리(불호랑이)와 붙으면 상당수가 사거리 밖에 서서 구경만 했다.

**원인:** `PetUnit` 의 접근 판정에서 *멈출지*(`arrived`)와 *때릴지*(`d ≤ areaR`)를
**서로 무관한 식**으로 재고 있었다. 멈추는 여유값 `bodyR × 0.6` 이 **자기 몸 크기에
비례**하는 바람에, 덩치가 클수록 제 사거리 밖에서 "다 왔다" 고 멈췄다. 멈추면 때리는
가지로 가는데 거기서 사거리 밖이라 안 때리고, 이미 도착했으니 더 걷지도 않는다 —
**걷지도 때리지도 않고 영영 선다.**

→ 고침: `arrived` 앞에 `d <= areaR` 를 AND 로 건다. 멈추는 조건은 **항상 때릴 수 있는
거리 안**이어야 한다. 앞으로 접근·정지 로직을 만질 때 이 관계를 깨지 말 것.

**★이 버그가 자글이전에서는 안 보였다는 게 교훈이다.** 근접인 자글이는 *제가 뛰어들어*
거리를 좁혀주므로 죽은 구간이 가려진다. 원거리는 제자리에서 쏘니 아무도 안 메워준다.
**한 판만 재면 이런 버그를 놓친다 — 삼각형 세 변을 다 돌려야 하는 이유다.**

### 축 셋은 서로 독립이다 — 하나를 만질 때 딸려오는 게 없는지 확인할 것

```
크기 등급(tier) → 크기 · 힘 · 체력
공격 방식(pattern) → 영역 모양 + 한 대 피해
역할(role)      → 공속 · 이속 · 사거리
인구수(supply)  → 몇 마리 나오나. 오직 그것만
```
2026-07-29 에 **같은 사고가 네 번** 났다: 인구수를 바꾸니 공격 범위가 바뀌고, 방식을
바꾸니 역할이 딸려오고, `pattern` 을 덮어쓰는 옛 줄이 남아 있었고, 포위가 원거리에도 걸렸다.
**한 축을 만지면 나머지 셋이 그대로인지 반드시 확인한다.**

**펫 행동을 다시 만들 때 쓸 부품** (2026-07-28 대삭제에서 살려 둔 것):
`Step`·`Face`·`Ground`·`Separate`·`MoveSpd`(이동) / `Die`·`DeathAnim`·`SpawnDrop`(죽음) /
`OnHit`·`HitFlash`·`TakeDamage`(피격) / `Airborne`·`Knock`(밖에서 거는 효과) /
`PetProjectile`(투사체) / 체력바 · 레벨 · 스폰

## 핵심 설계 요약 (정본은 game 저장소 문서)

- **조립 3층:** 코어(종=스탯) · 재질(행동) · 악세(패시브 효과, 펫당 1개)
- **스탯 4종:** 힘(물리딜) · 지력(마법딜+회복) · 민첩(공속+이동+회피) · 체력(순수 HP).
  **방어력 없음** — 데미지 감산 없이 표기대로 다 들어간다.
- **재질 = 스탯을 안 바꾸고 "무엇으로 발현되는지"만 정한다.**
  쇠(탱) · 나무(광역딜) · 고무(기동딜) · 유리(원거리딜, 물리/마법 양용) · 천(힐) · 자석(컨트롤)
- **스웜 ↔ 타이탄 상호 천적** (크기가 곧 상성). ★단 **편성 예산은 폐기** —
  위 「폐기」 참고. 크기는 *던졌을 때 몇 마리 나오나* 만 정하고, 데려갈 수 있는
  종류를 제한하지 않는다.
- **전투:** 펫은 전부 자동. 능동 입력은 꼬마에게만 — **WASD + 왼클릭 "잡기" 하나.**
  뭘 잡나 × 어디 놓나 × 홀드 × 잡고 이동 → 던지기·토템쌓기·줄다리기·매달리기·마인드컨트롤.
- **전투 길이:** 균등전 1~2분(TTK = 체력 ÷ 받는 딜). 치열함은 회복·흡혈·실드에서.
